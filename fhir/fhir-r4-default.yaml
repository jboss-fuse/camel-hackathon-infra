---
apiVersion: v1
kind: Service
metadata:
  name: fhir-r4-default
  labels:
    app: fhir-r4-default
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: fhir-r4-default
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fhir-r4-default
  labels:
    app: fhir-r4-default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fhir-r4-default
  template:
    metadata:
      labels:
        app: fhir-r4-default
    spec:
      containers:
        - name: fhir
          image: quay.io/fuse_qe/hapi-fhir:v8.2.0
          ports:
            - containerPort: 8080
              protocol: TCP
          env:
            - name: FHIR_VERSION
              value: "R4"
            - name: AUTH_ENABLED
              value: "false"
            - name: AUTH_USERNAME
              value: ""
            - name: AUTH_PASSWORD
              value: ""
          livenessProbe:
            httpGet:
              path: /fhir/metadata
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /fhir/metadata
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 5
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: fhir-r4-default
  labels:
    app: fhir-r4-default
spec:
  to:
    kind: Service
    name: fhir-r4-default
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
